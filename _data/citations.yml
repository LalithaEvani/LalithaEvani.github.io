# Citation counts cache (auto-generated upstream by the update-citations workflow,
# which is not enabled here). Empty is fine; publication citation badges simply
# will not render until you populate this or run that workflow.
{}
